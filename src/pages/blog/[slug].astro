---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { articles, getArticleBySlug, getRelatedArticles, type Article } from '../../data/articles';

export function getStaticPaths() {
  return articles.map((article) => ({
    params: { slug: article.slug },
    props: { article },
  }));
}

interface Props {
  article: Article;
}

const { article } = Astro.props;
const relatedArticles = getRelatedArticles(article.slug);

// Truncate title to fit within 60 chars after BaseLayout adds site suffix
// BaseLayout adds " | Pedestrian Accident Lawyer" (29 chars) if site name not present
const siteName = 'Pedestrian Accident Lawyer';
const rawTitle = article.metaTitle;
const maxTitleLength = rawTitle.includes(siteName) ? 60 : 60 - 29; // Account for suffix
const title = rawTitle.length > maxTitleLength ? rawTitle.substring(0, maxTitleLength - 3) + '...' : rawTitle;

const breadcrumbs = [
  { name: "Home", url: "/" },
  { name: "Blog", url: "/blog" },
  { name: article.title, url: `/blog/${article.slug}` }
];
---

<BaseLayout 
  title={title} 
  description={article.metaDescription}
  breadcrumbs={breadcrumbs}
>
  <article class="py-12">
    <div class="container-wide">
      <div class="grid lg:grid-cols-3 gap-8 lg:gap-12">
        <div class="lg:col-span-2">
      <div class="mb-8">
        <a href={`/blog/category/${article.category}`} class="text-sm font-semibold text-primary-600 uppercase">{article.categoryLabel}</a>
        <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 mt-2 mb-4">{article.title}</h1>
        <div class="flex items-center gap-4 text-gray-500 text-sm">
          <span>{article.readTime} min read</span>
          <span>•</span>
          <span>Published {article.publishDate}</span>
        </div>
      </div>

      <div class="prose prose-lg max-w-none">
        <p class="lead text-xl text-gray-600">{article.excerpt}</p>
        
        <div class="bg-primary-50 border-l-4 border-primary-500 p-6 my-8 rounded-r-lg">
          <h3 class="text-lg font-bold text-gray-900 mb-2">Were You Hit by a Car While Walking?</h3>
          <p class="text-gray-700 mb-4">Get a free case evaluation from an experienced pedestrian accident attorney. No fees unless you win.</p>
          <a href="/free-consultation" class="btn btn-primary">Get Your Free Case Review →</a>
        </div>

        <h2>Understanding {article.title}</h2>
        <p>This article covers important information about {article.title.toLowerCase()}. Pedestrian accidents are among the most devastating types of personal injury cases because victims have no protection from the impact of a vehicle.</p>

        <p>If you've been injured in a pedestrian accident related to {article.categoryLabel.toLowerCase()}, it's important to understand your legal rights and options for compensation.</p>

        <h2>Key Points to Remember</h2>
        <ul>
          <li>Seek medical attention immediately after any pedestrian accident</li>
          <li>Document everything at the scene if you're able</li>
          <li>Don't give statements to insurance companies without legal advice</li>
          <li>Understand your state's statute of limitations for filing claims</li>
          <li>Consider consulting with an experienced pedestrian accident attorney</li>
        </ul>

        <h2>Your Legal Rights After a Pedestrian Accident</h2>
        <p>Pedestrians generally have strong legal protections under state laws. Drivers have a duty to exercise reasonable care and watch for pedestrians, especially in crosswalks and at intersections.</p>

        <p>Even if you were partially at fault for the accident, you may still be able to recover compensation in most states under comparative negligence laws.</p>

        <div class="bg-gray-100 p-6 my-8 rounded-lg">
          <h3 class="text-lg font-bold text-gray-900 mb-2">Need Legal Help?</h3>
          <p class="text-gray-700 mb-4">Our network of experienced pedestrian accident attorneys offers free consultations. There's no fee unless you win your case.</p>
          <a href="/free-consultation" class="btn btn-cta">Start Your Free Evaluation</a>
        </div>
      </div>

          {relatedArticles.length > 0 && (
            <div class="mt-12 pt-8 border-t border-gray-200">
              <h2 class="text-2xl font-bold text-gray-900 mb-6">Related Articles</h2>
              <div class="grid md:grid-cols-3 gap-4">
                {relatedArticles.map(related => (
                  <a href={`/blog/${related.slug}`} class="bg-gray-50 rounded-lg p-4 hover:bg-gray-100 transition">
                    <span class="text-xs text-primary-600 uppercase">{related.categoryLabel}</span>
                    <h3 class="font-semibold text-gray-900 mt-1">{related.title}</h3>
                  </a>
                ))}
              </div>
            </div>
          )}
        </div>

        <!-- Sidebar with Lead Capture Form -->
        <div class="lg:col-span-1">
          <div class="bg-gradient-to-br from-primary-600 to-primary-700 rounded-xl shadow-lg p-6 md:p-8 sticky top-24">
            <div class="text-center mb-6">
              <div class="inline-flex items-center justify-center w-16 h-16 bg-white/20 rounded-full mb-4">
                <svg class="w-8 h-8 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <circle cx="12" cy="5" r="2" />
                  <path d="M12 7v5M9 20l3-8 3 8M9 14h6" />
                </svg>
              </div>
              <h3 class="text-2xl font-bold text-white mb-2">Free Case Review</h3>
              <p class="text-primary-100">Were you injured as a pedestrian? Find out what your case is worth.</p>
            </div>
            
            <form class="space-y-4" action="https://services.leadconnectorhq.com/hooks/OsNgWuy8oZzLbp5BXbnD/webhook-trigger/mva-site-lead" method="POST">
              <input type="hidden" name="source" value={`pedestrianaccidentlawyer-blog-${article.slug}`} />
              <input type="hidden" name="article" value={article.slug} />
              <input type="text" name="fullName" placeholder="Full Name" class="w-full px-4 py-3 rounded-lg bg-white/95 text-gray-900" required />
              <input type="tel" name="phone" placeholder="Phone Number" class="w-full px-4 py-3 rounded-lg bg-white/95 text-gray-900" required />
              <input type="email" name="email" placeholder="Email Address" class="w-full px-4 py-3 rounded-lg bg-white/95 text-gray-900" required />
              <select name="accidentTime" class="w-full px-4 py-3 rounded-lg bg-white/95 text-gray-900" required>
                <option value="">When did the accident occur?</option>
                <option value="within-week">Within the last week</option>
                <option value="within-month">Within the last month</option>
                <option value="within-6months">Within the last 6 months</option>
                <option value="within-year">Within the last year</option>
                <option value="over-year">Over a year ago</option>
              </select>
              <textarea name="description" placeholder="Briefly describe your accident..." rows="3" class="w-full px-4 py-3 rounded-lg bg-white/95 text-gray-900 resize-none"></textarea>
              <button type="submit" class="w-full bg-gray-900 hover:bg-gray-800 text-white font-bold py-4 px-6 rounded-lg transition text-lg">
                Get My Free Evaluation
              </button>
            </form>
            
            <div class="mt-6 text-center">
              <p class="text-primary-100 text-sm">🔒 Your information is confidential.</p>
              <p class="text-white font-semibold mt-2">Or call: </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </article>
</BaseLayout>
